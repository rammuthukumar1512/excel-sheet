// import type { Cell } from './cell';

// export interface GridSnapshot {
//   rows: number;
//   cols: number;
//   cells: Partial<Record<string, Cell>>; // keyed by `${r}:${c}` for sparse storage
// }

// export type CellKey = `${number}:${number}`;
